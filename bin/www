import app from "../app.js";
import dbConnect from "../db_connection.js";

const PORT = process.env.PORT || 3000;

dbConnect()
  .then(() => {
    app.listen(PORT, () => {
      console.log(`Server running on port: ${PORT} for dev`);
    });
  })
  .catch((err) => console.log(err));
